node_modules
.next
dist
tsconfig.tsbuildinfo

# Secretos: el patron con * cubre tambien copias tipo .env.local.bak,
# que antes quedaban afuera del ignore y podian colarse en un `git add .`
.env*
*.bak
.mcp.json

# Exports de sesiones de chat: pesados y suelen tener claves pegadas adentro
*.txt
